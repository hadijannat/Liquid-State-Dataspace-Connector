name: Proof Nightly

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

jobs:
  proof-nightly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@nightly
      - name: Install cargo-risczero
        run: cargo install cargo-risczero --version 5.0.0-rc.1 --locked
      - name: Run feature-gated proof-plane tests
        run: cargo test -p proof-plane-host --features risc0
      - name: Run feature-gated control API lineage test
        run: cargo test -p control-plane-api --features risc0 --test risc0_http_tests
